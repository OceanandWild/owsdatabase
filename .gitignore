# REGLA OPERATIVA (IMPORTANTE):
# No excluir proyectos completos aqui solo para 'evitar push'.
# Si un proyecto no debe deployarse, usar git add selectivo (allowlist),
# no reglas de ignore por carpeta/proyecto.

# ==========================================
# CONFIGURACIÃ“N DE PRIVACIDAD Y ORDEN VISUAL
# ==========================================

# 1. NO IGNORAR LOS ARCHIVOS DE CÃ“DIGO
# (Esto permite que el @ de VS Code y Antigravity los encuentre siempre)

# 2. IGNORAR SOLO CARPETAS PESADAS O BINARIOS
node_modules/
.env
.git/
.agent/
.gemini/
*.log
**/assets/
**/images/
**/videos/
**/audio/
**/*.png
**/*.jpg
**/*.mp4
**/*.zip

# 3. SEGURIDAD ADICIONAL:
# Aunque ahora estÃ¡n en el Ã­ndice (tracked), el comando 'assume-unchanged'
# evita que Git detecte cambios en ellos a menos que hagamos 'git add' explÃ­cito.
# Esto mantiene los proyectos en Blanco (No Verdes) en la mayorÃ­a de temas.

# 4. EXCEPCIÃ“N: LO QUE SÃ SE SUBE
!/server.js
!/Ocean Pay/
!/package.json
!/package-lock.json
!/README.txt
!/.gitignore
!/migrate_ebs.js
!/implementation_summary.md

# 5. FLORET SHOP (Android CI liviano)
# Guardamos solo el nucleo web/config. El resto se genera en CI.
Floret Shop/node_modules/
Floret Shop/android/
Floret Shop/.gradle/
Floret Shop/www/
Floret Shop/assets/
Floret Shop/*.log
Floret Shop/**/.DS_Store

!Floret Shop/
!Floret Shop/index.html
!Floret Shop/package.json
!Floret Shop/package-lock.json
!Floret Shop/capacitor.config.json
!Floret Shop/assets/
Floret Shop/assets/*
!Floret Shop/assets/floretshopicon.png
!Floret Shop/assets/floretshopicon.ico

# 6. OWS STORE (Android CI liviano)
OWS Store/node_modules/
OWS Store/android/
OWS Store/.gradle/
OWS Store/www/
OWS Store/assets/
OWS Store/*.log
OWS Store/**/.DS_Store

!OWS Store/
!OWS Store/index.html
!OWS Store/package.json
!OWS Store/package-lock.json
!OWS Store/capacitor.config.json
!OWS Store/resources/
!OWS Store/resources/icon.png

# 7. OCEAN PAY (icono Android versionado para CI)
!Ocean Pay/
!Ocean Pay/build/
!Ocean Pay/build/icon.png


