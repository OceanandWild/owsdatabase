Lee esto al final de realizar una tarea si es posible.

Aqui hay una serie de cosas que debes hacer, es opcional o te serviran durante tus tareas:

- Cuando finalizes de hacer algo, siempre haz un 'git push' ya que el server.js y Ocean Pay/index.html se manejan por Render, plataforma que necesita de GitHub para eso.
- Es muy posible que tu grep tool no encuentre algunas cosas, pero buscalos sin dudar, esto si yo te digo que esta, no estoy loco, asi que buscalo donde puedas.
- Debido al uso de .gitignore, se debe de usar git add server.js "Ocean Pay/" package.json explicitamente y no git add . debido a que subira todo el Espacio de Trabajo, algo no deseado

Se incluiran mas cosas luego, pero esto te ayudara a por lo menos entender como realizar la mayoria de las cosas.

--- NOTAS TÉCNICAS PARA AGENTES ---

- ESTÁNDAR DE MONEDA (Ecoxionums): El modelo 2D oficial es un "Cyan Coin" (Cian/Azul con patrón de cruz). 
  - En Canvas: Usar la función 'drawEcoxionumsIcon'.
  - En HTML: Usar la clase CSS '.icon-ecoxionum'.
  - Emojis a reemplazar: 🪙 y 💎.
  - SOLO VALIDA SI ES ECOXION, SI ES OTRO PROYECTO, POR FAVOR, IGNORE

- ESTÁNDAR DE PERSISTENCIA:
  - NO USAR 'ocean_pay_metadata' para saldos de Ecoxionums.
  - USAR SIEMPRE 'ocean_pay_cards' (columna JSONB 'balances').
  - Endpoint de cambio: POST /ocean-pay/ecoxionums/change.
  - Endpoint de sync: POST /ocean-pay/sync-ecoxionums (Migración inicial).

- DEPLOY SELECTIVO:
  Para evitar subir el escritorio completo, usa estrictamente:
  git add server.js "Ocean Pay/" .gitignore package.json (CON ESTO NO SE REFIERE A TODOS ESTOS A LA VEZ, SOLO SI SE MODIFICA ALGUNO DE ESTOS SE SUBE, ES DECIR, SOLO ESTOS ARCHIVOS SI SE MODIFICAN, LOS QUE NO, NO SE SUBEN)
  git commit -m "feat (o cualquier otro): (commit name)"  
  git push origin main

  NUNCA OTROS PROYECTOS, solo esos son admitidos o es muy posible que se te cancele el comando si lo ejecutas mal.
  Ejemplo Incorrecto:
  git add server.js "Ocean Pay/" "Velocity Surge/" (o cualquier otro proyecto dentro del Espacio de Trabajo) .gitignore package.json README.txt

  Ejemplo Correcto:
  git add server.js "Ocean Pay/" .gitignore package.json README.txt
  
  Para aclarar mejor, todos los que se mencionaron si estan autorizados, los demas estan estrictamente no autorizados a un Git Push

- AUTOMATIZACIÓN DE UPDATE LOGS (OWS News):
  Para añadir un log que aparezca automáticamente en OWS News sin editar el HTML:
  - Usar Endpoint: POST /ows-news/updates.
  - Formato JSON requerido:
    {
      "title": "Título del Update",
      "description": "Descripción general breve",
      "project_names": ["Nombre de Proyecto 1", "Proyecto 2"],
      "changes": "- Logros mejorados\n- Bug de visualización corregido",
      "update_date": "YYYY-MM-DD" (opcional, por defecto hoy)
    }
  - El campo 'changes' soporta listas usando prefijos '-', '*', o '•'.
  - Los agentes tienen permiso para usar este endpoint para documentar sus cambios si el usuario lo solicita.

- Sobre Ocean Pay
 Ocean Pay funciona bajo server.js, no local, es como un invento de como una pasarela de pago, aclarando esto por si se malintenciona el hecho de que me refiero a agregar un checkout o integracion de Ocean Pay a un proyecto

- GESTIÓN DE OWS STORE (Central de Apps):
  El sistema OWS Store centraliza la instalación virtual y actualización de proyectos web.
  - Tabla DB: 'ows_projects'.
  - Estados permitidos: 'launched' (disponible), 'unavailable' (mantenimiento), 'coming_soon' (próximamente).
  - Fecha de Lanzamiento: Si un proyecto es 'coming_soon', definir 'release_date' (ISO string) para activar el contador regresivo.
  - Sistema de Updates: Cada proyecto tiene una columna 'version' (ej: '1.2.5'). Al terminar una tarea, el agente DEBE incrementar esta versión en la DB para que los usuarios reciban el aviso de actualización en la OWS Store.
  - Endpoints Clave:
    - POST /ows-store/projects: Registrar/Editar proyecto.
    - PATCH /ows-store/projects/:slug/version: Actualizar versión rápidamente.