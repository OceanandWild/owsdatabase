# Carpetas de sistema y dependencias (NUNCA subir)
node_modules/
.env
uploads/
.vscode/
.gemini/
.agent/
.git/

# Archivos temporales
*.log
.DS_Store

# NOTA: He eliminado el '*' para que tus proyectos no se vean en gris y puedas usar @ con ellos.
# RECUERDA: Para no subir todo el workspace al servidor, usa solo:
# git add server.js "Ocean Pay/" package.json
# Evita usar 'git add .' a menos que quieras subir nuevos proyectos.
