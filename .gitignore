# ==========================================
# CONFIGURACIÓN DE PRIVACIDAD Y ORDEN VISUAL
# ==========================================

# 1. NO IGNORAR LOS ARCHIVOS DE CÓDIGO
# (Esto permite que el @ de VS Code y Antigravity los encuentre siempre)

# 2. IGNORAR SOLO CARPETAS PESADAS O BINARIOS
node_modules/
.env
.git/
.agent/
.gemini/
*.log
**/assets/
**/images/
**/videos/
**/audio/
**/*.png
**/*.jpg
**/*.mp4
**/*.zip

# 3. SEGURIDAD ADICIONAL:
# Aunque ahora están en el índice (tracked), el comando 'assume-unchanged'
# evita que Git detecte cambios en ellos a menos que hagamos 'git add' explícito.
# Esto mantiene los proyectos en Blanco (No Verdes) en la mayoría de temas.

# 4. EXCEPCIÓN: LO QUE SÍ SE SUBE
!/server.js
!/Ocean Pay/
!/package.json
!/package-lock.json
!/README.txt
!/.gitignore
!/migrate_ebs.js
!/implementation_summary.md

# 5. FLORET SHOP (Android CI liviano)
# Guardamos solo el nucleo web/config. El resto se genera en CI.
Floret Shop/node_modules/
Floret Shop/android/
Floret Shop/.gradle/
Floret Shop/www/
Floret Shop/assets/
Floret Shop/*.log
Floret Shop/**/.DS_Store

!Floret Shop/
!Floret Shop/index.html
!Floret Shop/package.json
!Floret Shop/package-lock.json
!Floret Shop/capacitor.config.json

# 6. OWS STORE (Android CI liviano)
OWS Store/node_modules/
OWS Store/android/
OWS Store/.gradle/
OWS Store/www/
OWS Store/assets/
OWS Store/*.log
OWS Store/**/.DS_Store

!OWS Store/
!OWS Store/index.html
!OWS Store/package.json
!OWS Store/package-lock.json
!OWS Store/capacitor.config.json
