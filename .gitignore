# ==========================================
# CONFIGURACIÓN DE PRIVACIDAD Y ORDEN VISUAL
# ==========================================

# 1. Ignorar todo por defecto
*

# 2. PERMITIR CARPETAS RAÍZ (Las pone en BLANCO/Normal en el sidebar)
# Esto permite que el @ funcione con los nombres de las carpetas.
!*/

# 3. PROTEGER CONTENIDO PRIVADO (Pone el interior en GRIS e INVISIBLE para Git)
# NADA de lo que metas en las carpetas de proyectos se subirá a GitHub.
/*/*

# 4. EXCEPCIÓN: Ocean Pay y Archivos del Servidor (Lo que SÍ queremos en GitHub)
!Ocean Pay/
!Ocean Pay/**
!server.js
!package.json
!package-lock.json
!README.txt
!.gitignore
!migrate_ebs.js

# 5. REGLAS DE SEGURIDAD ESTRICTAS (NUNCA SUBIR)
node_modules/
.env
.git/
.vscode/
.agent/
.gemini/
*.log
.DS_Store
**/assets/
**/images/
**/videos/
**/audio/
**/*.png
**/*.jpg
**/*.mp4
**/*.zip
